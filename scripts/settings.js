/* This file defines the global settings object, used to store user settings */

let settings =
{
    // all bounds are inclusive
    min: -5,
    max: 5,
    minDim: 2,
    maxDim: 3,
    genShortcut: "g",
    solShortcut: "s",
    sbsShortcut: "t",
}